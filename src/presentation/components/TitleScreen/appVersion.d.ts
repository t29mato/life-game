/**
 * Injected by Vite's `define` (see `vite.config.ts`) straight from
 * `package.json`'s `version` field, so the build shown on screen can never
 * drift from the package that produced it. Replaced with a string literal at
 * build time — never a runtime import.
 */
declare const __APP_VERSION__: string
